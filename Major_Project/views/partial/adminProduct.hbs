{{#each this.products as |product|}}
<div class="productItem">
    <div class="image" style="background-image: url({{product.imageUrl}})">
    </div>

    <div class="info">
        <a class="productInfoLink" href="/shop/details?id={{product._id}}" target="_blank">
            <div class="productInfo">
                <h2 class="name">
                    {{product.name}}
                </h2>
                <ul class="descriptionList">
                    {{#each product.description as |desc|}}
                    <li>{{desc}}</li>
                    {{/each}}
                </ul>
            </div>
        </a>
    </div>

    <div class="priceInfo">
        <div>
            $ {{product.price}}
        </div>
    </div>
    <div class="btnGroup">
        <button>
            <a href="/admin/edit?id={{product._id}}">Edit</a>
        </button>
        <button>
            <a href="/admin/delete?id={{product._id}}">Delete</a>
        </button>
    </div>
</div>
{{/each}}